import 'package:flutter/material.dart';

class KpMobileBody extends StatelessWidget {
  const KpMobileBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.deepPurple[300],
    );
  }
}